export default interface PostViewState {
    liked: boolean
    likesCounter: number
    likedUsers: number[]
    userId: number
    content: string
    text: string
    id: number
    username: string
    userPic: string
}